# PSEUDOCODE DRAFT: this file is not consumed by the production composite action.
# Use examples/app-repository/.github/ios-build.yml for the implemented schema.

apps:
  - name: ExampleApp
    bundle_id: com.example.app
    scheme: ExampleApp
    lane: build
    export_method: app-store

  - name: ExampleApp-Staging
    bundle_id: com.example.app.staging
    scheme: ExampleApp-Staging
    lane: build
    export_method: ad-hoc
