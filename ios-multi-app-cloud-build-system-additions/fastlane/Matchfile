# PSEUDOCODE DRAFT: values must be supplied by the App repository CI environment.

git_url(ENV.fetch("MATCH_GIT_URL"))
type(ENV.fetch("MATCH_TYPE", "appstore"))
storage_mode("git")

# The private match repository stores encrypted signing material. ASC API keys
# are separate and must never be stored in the match repository.
